import { ServiceBottomNav } from "./ServiceBottomNav";
import { View } from "react-native";

export const ServiceBnav=()=>{
    return(
        <View style={{minHeight:'100%',minWidth:'100%'}}>
            <ServiceBottomNav/>
        </View>
    );
}