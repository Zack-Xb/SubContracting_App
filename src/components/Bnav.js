import BottomNav from "./BottomNav";
import { View } from "react-native";

export const Bnav=()=>{
    return(
        <View style={{minHeight:'100%',minWidth:'100%'}}>
            <BottomNav/>
        </View>
    );
}